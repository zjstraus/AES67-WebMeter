name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-go@v1.0.0
      with:
        version: 1.13
    - name: Build the server
      run: go build .
